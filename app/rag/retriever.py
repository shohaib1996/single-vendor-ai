# Retriever factory — plan.txt section 4 + section 5 (rag_search tool)
#
# TODO:
#   def get_retriever(top_k=4, metadata_filter=None) -> a LangChain
#       retriever over the PGVector store from vectorstore.py
